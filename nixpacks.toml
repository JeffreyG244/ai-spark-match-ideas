providers = ['node']

[variables]
NODE_ENV = 'production'

[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]  
cmds = ['cd railway-deploy && npm ci']

[start]
cmd = 'cd railway-deploy && node server.js'