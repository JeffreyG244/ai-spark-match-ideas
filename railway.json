{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd railway-deploy && npm ci"
  },
  "deploy": {
    "startCommand": "cd railway-deploy && node server.js"
  },
  "variables": {
    "NODE_ENV": "production"
  }
}